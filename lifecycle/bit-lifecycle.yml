generated: '2026-07-18'
method: searched
source: https://github.com/teambit/bit/releases
description: >-
  Bit versioning and operational lifecycle. Bit itself is released under SemVer
  and distributed as bundled versions via BVM (Bit Version Manager), which lets
  developers install and pin specific Bit versions. Components authored with Bit
  carry their own SemVer tags/snaps and can be marked deprecated with
  `bit deprecate` (component-level, not an API deprecation policy).
versioning:
  scheme: semver
  current: v2.0.11
  manager: "@teambit/bvm"
  docs: https://bit.dev/reference/reference/using-bvm/
changelog: https://github.com/teambit/bit/releases
status_page: https://status.bit.cloud
component_deprecation:
  command: bit deprecate <component-pattern>
  scope: component-level
  note: Bit publishes no formal API deprecation / Sunset-header policy for the hosted platform.
